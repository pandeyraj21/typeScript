//tuples is used for follow the order of the data

let tUser:[string,number,boolean]=['rf',22,true]   ///here we can't change the order it is use full for api's call where order is mandatory.

//bug is that we can push any value that can break the order.